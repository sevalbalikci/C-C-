// Framework

#include <stdbool.h>

// Merges two arrays into one.
void concatArrays(int *num1, int len1, int *num2, int len2);

// String in reversed order
void revOrd(char *input, char *rev);

// Compare strings and return 1 if strings are the same, 0 if not
int compareStrings(char *input, char *rev);

// Checks if integer is palindrome. Return true if it is palin
bool isPalindrome(int x);

// Returns the length of an integer
int intLength(int input);

// Armstrong Number
int armstrongNum(int input);

// Reverses the order of an integer
int revInt(int *a);

// Returns the calculation with a negative number
int intNegExpo(int* exponent, int* base);

// Returns the calculation with a positive number
int intPosExpo(int* exponent, int* base);

// Factorial of a number
int fact(int num);

// Return true if its a leap year
bool isLeapYear(int* year);

// Reverses integer array
int revIntArray(int *arr, int len);